{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: clang++ build active file",
      "command": "/usr/local/Cellar/llvm/11.0.0_1/bin/clang++", // "/usr/bin/clang++", "/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang++", "/usr/bin/g++",
      "args": [
        // "-v",
        "-std=c++17",
        "-stdlib=libc++",

        "-isysroot",
        "/Library",

        "-isysroot",
        "/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk",

        "-g",
        "${file}",

        // "-includes",
        // "${workspaceFolder}/src/calc_percent.cpp",
        // "${workspaceFolder}/src/constants.cpp",
        // "${workspaceFolder}/src/icosahedron.cpp",
        // "${workspaceFolder}/src/phex.cpp",
        // "${workspaceFolder}/src/point3.cpp",
        // "${workspaceFolder}/src/triangle.cpp",

        "-include",
        "${workspaceFolder}/src/**.cpp",

        "-o",
        "${workspaceFolder}/builds/${fileBasenameNoExtension}"
        // "-v"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "compiler: /usr/local/Cellar/llvm/11.0.0_1/bin/clang++"
    }
  ]
}

// TODO: try adding flags for strict math so follows IEEE754 (no rounding, unsafe operations, etc.)
